name: create version

on:
  create:
    branches:
      - main
    tags:
      - v/*.*.*

jobs:
  echo:
    runs-on: ubuntu-latest

    steps:
      - name: Echo
        run: |
             echo "Tag name from GITHUB_REF_NAME: $GITHUB_REF_NAME"
